import { walls } from "./walls.js";
import { paths } from "./paths.js"
export class fruits {


    constructor()
    {
        // where we will remove the fruits if theres walls
    }


        fruitParameters = {
        fruitsFromLeftToRight: 20,
        fruitsFromTopToBottom: 15,
        height: 10,
        width: 10,
        spacePerFruit: 0
      }


    fruitEaten()
    {
        //every time pacman moves, this funtion is called to remove the fruit pacman is on (if any)
    }
    isWall(ctx)
    {
        let x = 0
        let y = 0
        for (i = 1; wall.walls; ++i)
        {
            if (wall.walls.i.start.x == x){}
            else{ if(wall.walls.i.start.y == y){}
                else{ if (wall.walls.i.end.x == x){}
                    else{ if (wall.walls.i.end.y == y){}
                        else
                        paths.createPath.drawFruit()
            
            
                
                
            
                             
                  
                        
        }                    
        }                 
        }
        }   
    }  
}
